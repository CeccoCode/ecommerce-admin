import { SpinnerDiamond } from "spinners-react";

export default function Spinner() {
    return (
        <SpinnerDiamond size={48} thickness={109} speed={100} color="rgba(30, 58, 138, 1)" secondaryColor="rgba(0, 0, 0, 1)" />
    );
}